#include <bits/stdc++.h>
using namespace std;

int main(){
    int T;
    cin >> T;

    while(T--){
        int N;
        cin >> N;

        map<int, int> monster;

        while(N--){
            int a, b;
            cin >> a >> b;
            
            monster.insert({a, b});
        }
    }
}